# External Access Configuration

# For ngrok or custom domains, set the HMR host
# HMR_HOST=your-ngrok-domain.ngrok.io
# HMR_PORT=443
# HMR_PROTOCOL=wss

# For HTTPS ngrok tunnels
# HMR_PROTOCOL=wss

# VAPID keys for push notifications (generate with: npm run generate-vapid)
# VITE_VAPID_PUBLIC_KEY=your_vapid_public_key
# VAPID_PRIVATE_KEY=your_vapid_private_key

# Example configurations:
# Local development: (default, no env vars needed)
# LAN access: (no env vars needed, will work automatically)
# ngrok HTTP: HMR_HOST=abc123.ngrok.io
# ngrok HTTPS: HMR_HOST=abc123.ngrok.io HMR_PROTOCOL=wss HMR_PORT=443
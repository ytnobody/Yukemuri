{
  "name": "my-yukemari-app",
  "version": "1.0.0",
  "description": "A new Yukemari application",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build --mode client && vite build",
    "dev": "vite dev",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy ./dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/node-server": "^1.19.0",
    "preact": "^10.0.0",
    "preact-render-to-string": "^6.0.0",
    "@preact/signals": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "@preact/preset-vite": "^2.0.0",
    "@hono/vite-dev-server": "^0.11.0",
    "wrangler": "^3.0.0"
  }
}
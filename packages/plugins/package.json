{
  "name": "plugins-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Plugins are built individually by their own package.json'",
    "dev": "echo 'Plugins are developed individually by their own package.json'",
    "test": "echo 'Plugins are tested individually by their own package.json'",
    "lint": "echo 'Plugins are linted individually by their own package.json'",
    "type-check": "echo 'Plugins are type-checked individually by their own package.json'",
    "clean": "echo 'Plugins are cleaned individually by their own package.json'"
  }
}